# Research: LLM Post-Processing สำหรับ Thai STT

**วันที่:** 28 กุมภาพันธ์ 2026
**หัวข้อ:** ใช้ LLM เกลาข้อความจาก Whisper STT โดยเฉพาะภาษาไทย

---

## 1. ปัญหาหลักของ STT ภาษาไทย

### 1.1 ประเภทข้อผิดพลาดที่พบบ่อย

| ประเภทข้อผิดพลาด | ตัวอย่าง | สาเหตุ |
|-----------------|---------|--------|
| **Tone Mark Error** | ม้า → มา, ข้าว → ขาว | 5 วรรณยุกต์ไทย ฟังเหมือนกัน |
| **Homophone Confusion** | กาน → กาล, เสริม → เสิร์ม | เสียงพ้อง ต่างความหมาย |
| **Fast Speech** | "ไม่รู้ว่าจะทำยังไง" → "ไม่รู้จะยังไง" | ตัดพยางค์ที่ออกเสียงเบา |
| **Code-switching** | "ผม submit งาน แล้ว" → "ผมซับมิดงานแล้ว" | Thai-English ปนกัน |
| **Filler Words** | "เอ่อ... อ่า... ครับ ผมคิดว่า" | คำอุทาน ควรตัดออก |
| **No Punctuation** | ข้อความยาวไม่มีจุดไม่มีลูกน้ำ | Whisper ไม่ใส่เครื่องหมายวรรคตอน |
| **Domain Vocab** | "ไฮเปอร์เทนชัน" → "ไฮเปอเทนชั่น" | คำศัพท์เฉพาะทาง |

### 1.2 ทำไม WER ไม่เหมาะกับภาษาไทย

- ภาษาไทยไม่มี word boundary (ไม่มีช่องว่างระหว่างคำ)
- WER ที่ถูกต้องสมบูรณ์ยังอาจได้ 100% error เพราะ word segmentation ต่างกัน
- **✅ ใช้ CER (Character Error Rate)** แทน — วัดที่ระดับอักขระ
- อ้างอิง: "Advocating Character Error Rate for Multilingual ASR Evaluation" (NAACL 2025)

---

## 2. งานวิจัยหลัก: LLM Post-Processing สำหรับ STT

### 2.1 HyPoradise (NeurIPS 2023) — Benchmark มาตรฐาน

**เป้าหมาย:** Dataset และ benchmark มาตรฐานสำหรับ LLM-based ASR correction

**ผลลัพธ์สำคัญ:**
- Dataset 316,000+ คู่ (N-best hypotheses + correct transcription)
- **Generative correction ดีกว่า ranking-based** อย่างชัดเจน
- LLM สามารถสร้างคำที่ถูกต้องซึ่งไม่มีใน N-best list เลย

**ArXiv:** https://arxiv.org/abs/2309.15701

---

### 2.2 Whispering LLaMA (EMNLP 2023) — Cross-Modal Approach

**เป้าหมาย:** รวม Whisper encoder (เสียง) + LLaMA decoder (ภาษา) เข้าด้วยกัน

**ผลลัพธ์:**
- **37.66% relative WER improvement** เทียบกับ N-best rescoring
- ใช้ acoustic embedding จาก Whisper ประกอบกับ LLM correction
- Parameter-efficient fine-tuning

**GitHub:** https://github.com/Srijith-rkr/Whispering-LLaMA

**ความเกี่ยวข้องกับโปรเจค:**
- แนวคิดนี้ใช้ได้กับ Thai: Whisper encoder → Typhoon/OpenThaiGPT decoder
- แต่ซับซ้อนกว่า — สำหรับ MVP ใช้ simple LLM correction ก่อน

---

### 2.3 Task-Activating Prompting (Amazon Science 2023)

**เป้าหมาย:** Prompt engineering ที่ดีขึ้นสำหรับ STT correction

**Key Innovation:** บอก LLM ชัดๆ ว่า "คุณกำลังแก้ไข STT errors" — ไม่ใช่แค่ส่งข้อความไปให้แปล

**Prompt pattern ที่ได้ผล:**
```
You are a speech recognition error correction system.
The following text was transcribed from audio and may contain errors.
Correct spelling, grammar, and unclear words while preserving the speaker's intent.
```

**ArXiv:** https://assets.amazon.science/77/26/...

---

### 2.4 ผลการทดสอบจากงานวิจัยรวม

| Approach | WER Improvement |
|----------|----------------|
| N-best rescoring (traditional) | 5-15% |
| LLM prompt correction (zero-shot) | 15-30% |
| LLM + few-shot examples | 25-40% |
| LLM + context window | 35-50% |
| Cross-modal (Whispering LLaMA) | 37-50%+ |

**สำหรับภาษาไทย คาดว่า CER improvement: 40-60%** จาก STT-only → STT + LLM

---

## 3. Context Window Strategy

### 3.1 หลักการ: ทำไม Context ช่วยได้

```
ตัวอย่าง: ผู้ใช้กำลังเขียนรายงานการแพทย์

❌ ไม่มี context:
   STT: "ผู้ป่วยมีความดัน 140 ต่อ 90 มีอาการ ไฮเปอเทนชั่น"
   → LLM ไม่รู้ว่านี่คือ domain การแพทย์

✅ มี context (2-3 ประโยคก่อนหน้า):
   Context: "บันทึกการตรวจผู้ป่วย... โรคหัวใจและหลอดเลือด..."
   STT: "ผู้ป่วยมีความดัน 140 ต่อ 90 มีอาการ ไฮเปอเทนชั่น"
   → LLM แก้เป็น: "ผู้ป่วยมีความดัน 140/90 มีอาการ Hypertension"
```

### 3.2 Sliding Window Approach (แนะนำ)

```
Session: [s1][s2][s3][s4][s5]...
                   ↑
               ปัจจุบัน

Context ที่ส่งให้ LLM = [s3][s4] (2 ประโยคก่อนหน้า)
```

**งานวิจัยพบว่า:**
- **2-3 ประโยคก่อนหน้า** ให้ผลดีที่สุด (diminishing returns หลังจากนั้น)
- เก็บ context ไว้ในหน่วยความจำตลอด session
- ล้าง context เมื่อ user เริ่ม topic ใหม่ (ตรวจจับจาก pause ยาว หรือ explicit command)

### 3.3 Domain Detection

ตรวจจับ domain อัตโนมัติจาก context แล้วใส่ใน prompt:

```rust
enum Domain {
    Medical,    // ตรวจจับจาก: ผู้ป่วย, ยา, อาการ, โรค
    Legal,      // ตรวจจับจาก: สัญญา, กฎหมาย, ข้อพิพาท
    Technical,  // ตรวจจับจาก: code, function, deploy, server
    Casual,     // default
}
```

---

## 4. Local LLM สำหรับ Thai (Lightweight)

### 4.1 เปรียบเทียบตัวเลือก

| Model | ขนาด | Thai Support | RAM (Q4) | Speed (CPU) | แนะนำสำหรับ |
|-------|------|-------------|---------|------------|-------------|
| **Typhoon2-Qwen2.5-7B** | 7B | ⭐⭐⭐⭐⭐ Thai-specialized | ~5GB | ช้าบน CPU | GPU users |
| **OpenThaiGPT 1.5-7B** | 7B | ⭐⭐⭐⭐⭐ Thai-native | ~5GB | ช้าบน CPU | GPU users |
| **Qwen2.5-3B** | 3B | ⭐⭐⭐⭐ (29 languages) | ~2.5GB | ✅ ใช้ได้ | **Balance** ✅ |
| **Llama 3.2-3B** | 3B | ⭐⭐⭐⭐ Thai confirmed | ~2.5GB | ✅ ใช้ได้ | Alternative |
| **Qwen2.5-1.5B** | 1.5B | ⭐⭐⭐ | ~1.2GB | ✅ เร็ว | Low-end PC |
| **Gemma 3-1B** | 1B | ⭐⭐⭐ (140 languages) | ~0.9GB | ✅ เร็วมาก | Minimal RAM |
| **phi-4-mini (3.8B)** | 3.8B | ⭐⭐⭐ | ~3GB | ✅ ใช้ได้ | Reasoning tasks |

### 4.2 แนะนำสำหรับโปรเจคนี้

```
ตามสเปคเครื่อง:

CPU เท่านั้น (RAM < 8GB):
  → Qwen2.5-1.5B GGUF Q4 (เร็ว, Thai โอเค)

CPU เท่านั้น (RAM 8-16GB):
  → Qwen2.5-3B GGUF Q4 (balance ดีที่สุด) ✅

มี GPU (VRAM 6GB+):
  → Typhoon2-Qwen2.5-7B (Thai สูงสุด)
  → หรือ OpenThaiGPT 1.5-7B
```

### 4.3 Rust Integration

```toml
[dependencies]
# llama.cpp Rust bindings
llama_cpp = "0.3"

# หรือใช้ Ollama REST API (ง่ายกว่า)
reqwest = { version = "0.12", features = ["json"] }
```

**ตัวเลือก Integration:**

**Option A: llama.cpp binding (in-process)**
- ไม่ต้องรัน external service
- ใช้ `llama_cpp` crate
- Memory อยู่ใน process เดียวกัน

**Option B: Ollama REST API (แนะนำสำหรับ MVP)**
- User ติดตั้ง Ollama แยก
- Call `http://localhost:11434/api/generate`
- ง่ายกว่า, flexible กว่า
- รองรับสลับ model ได้

---

## 5. System Architecture: STT + LLM Pipeline

### 5.1 Flow การทำงาน

```
[Push-to-Talk]
     │
     ▼
[cpal: Audio Capture] ──── 16kHz mono f32
     │
     ▼
[VAD: Silero] ──── ตัด silence ออก
     │
     ▼
[whisper-rs: STT] ──── ~5-15s
     │ raw_text (อาจมีข้อผิดพลาด)
     ▼
[Context Manager]
  ├── rolling_window: Vec<String> (2-3 ประโยคล่าสุด)
  ├── domain: Domain (Medical/Legal/Tech/Casual)
  └── user_vocab: HashMap<String, String>
     │
     ▼
[LLM Correction] ──── ~1-3s (Qwen 3B CPU)
  Input: raw_text + context + domain + user_vocab
  Output: corrected_text
     │
     ▼
[Text Injection]
  arboard (clipboard) + enigo (Ctrl+V)
     │
     ▼
[Active Window]
```

### 5.2 Prompt Engineering สำหรับภาษาไทย

```
SYSTEM PROMPT:
คุณคือระบบแก้ไขข้อความจาก Speech-to-Text สำหรับภาษาไทย
หน้าที่: แก้ไขข้อผิดพลาดจากการถอดเสียง โดยรักษาความหมายเดิม

กฎ:
1. แก้ไขคำผิดพลาดจากการออกเสียง (เช่น วรรณยุกต์ผิด, คำพ้องเสียง)
2. ลบคำอุทาน (เอ่อ, อ่า, อ่านะ) ออก
3. เพิ่มเครื่องหมายวรรคตอนที่เหมาะสม
4. รักษา code/ศัพท์เทคนิค ไม่แปลงเป็นภาษาไทย
5. ตอบเฉพาะข้อความที่แก้ไขแล้ว ไม่ต้องอธิบาย

---
USER PROMPT:
domain: {domain}
คำศัพท์พิเศษ: {user_vocab}

บริบทก่อนหน้า:
{context_window}

ข้อความที่ถอดจากเสียง:
{raw_text}
```

### 5.3 Few-Shot Examples ใน Prompt (ตัวอย่าง)

```
ตัวอย่างการแก้ไข:
Input:  "เอ่อ ผม เสร็จ งาน แล้ว นะ ครับ จะ ส่ง ให้ พรุ่งนี้"
Output: "ผมเสร็จงานแล้ว จะส่งให้พรุ่งนี้"

Input:  "ไฟล์ มัน ไม่ โหลด เพราะ network connection มัน drop"
Output: "ไฟล์ไม่โหลดเพราะ network connection มัน drop"
```

---

## 6. Context Window Design (รายละเอียด)

### 6.1 Context Manager Structure

```rust
struct ContextManager {
    // Rolling window: เก็บ N ประโยคล่าสุด
    history: VecDeque<String>,
    max_history: usize,      // แนะนำ: 3

    // Domain detection
    domain: Domain,
    domain_keywords: HashMap<Domain, Vec<&'static str>>,

    // User custom vocabulary
    user_vocab: HashMap<String, String>,  // ผิด → ถูก

    // Session metadata
    session_start: Instant,
    last_utterance: Instant,
    silence_threshold: Duration,  // ถ้า pause > 30s → ล้าง context
}
```

### 6.2 Domain Detection Algorithm

```rust
fn detect_domain(text: &str, history: &[String]) -> Domain {
    let all_text = history.join(" ") + " " + text;

    let scores = [
        (Domain::Medical, vec!["ผู้ป่วย","ยา","อาการ","โรค","แพทย์","วินิจฉัย"]),
        (Domain::Legal,   vec!["สัญญา","กฎหมาย","ข้อพิพาท","คดี","ศาล"]),
        (Domain::Technical, vec!["code","function","server","deploy","bug","database"]),
    ];

    // นับคะแนนแต่ละ domain และเลือก domain ที่มีคะแนนสูงสุด
    // ถ้าต่ำกว่า threshold → Casual
}
```

### 6.3 User Vocabulary Learning

```rust
// บันทึกใน local file: ~/.config/thai-vtt/vocab.json
{
    "ไฮเปอเทนชั่น": "Hypertension",
    "ซีพียู": "CPU",
    "บริษัทของเรา": "บริษัท ABC จำกัด",
    "บอส": "คุณสมชาย"
}
```

User สามารถ:
- แก้ไขผ่าน UI ของ widget
- ระบบ auto-learn จาก correction history (future feature)

---

## 7. Performance Budget

### 7.1 Latency ต่อ utterance (10 วินาทีเสียง)

| ขั้นตอน | CPU (no GPU) | GPU |
|---------|-------------|-----|
| Audio capture | ~0ms | ~0ms |
| VAD (Silero) | ~50ms | ~20ms |
| Whisper STT (medium) | **5-15s** | **1-3s** |
| LLM Correction (Qwen 3B) | **2-5s** | **0.5-1s** |
| Text inject | ~10ms | ~10ms |
| **รวม** | **~8-20s** | **~2-4s** |

### 7.2 ปรับปรุง UX ระหว่างรอ

```
Strategy: แสดงผลแบบ 2 ขั้นตอน

1. แสดง raw STT ก่อน (ทันทีหลัง Whisper เสร็จ)
   → ผู้ใช้เห็นผลทันที แม้ยังไม่ clean

2. LLM แก้ไขใน background
   → อัปเดตข้อความใน widget
   → แทนที่ด้วย corrected version

UI:
┌─────────────────────────────┐
│ 🔴 กำลังฟัง...              │
│ ─────────────────────────── │
│ [raw] ผมเสร็จงานแล้วนะครับ  │
│ [✨]  กำลังเกลาคำ...         │
└─────────────────────────────┘
```

### 7.3 Memory Usage

| Component | RAM |
|-----------|-----|
| Whisper Medium | ~800MB |
| Qwen 3B GGUF Q4 | ~2.5GB |
| App overhead | ~50MB |
| **รวม** | **~3.5GB** |

> **ข้อแนะนำ:** ถ้าเครื่อง RAM น้อย ใช้ Whisper Small + Qwen 1.5B → รวม ~1.5GB

---

## 8. โหมดการทำงานที่ควรรองรับ

### Mode 1: Fast Mode (STT only)
- ไม่ผ่าน LLM
- เร็วที่สุด, latency ต่ำ
- เหมาะกับ casual use

### Mode 2: Standard Mode (STT + LLM correction)
- ผ่าน LLM เกลาคำ + ตัด filler words + เพิ่ม punctuation
- แนะนำสำหรับการใช้งานทั่วไป

### Mode 3: Context Mode (STT + LLM + rolling context)
- เพิ่ม context window
- เหมาะกับ long-form dictation, writing
- ความแม่นยำสูงสุด

---

## 9. Papers และ References

| Paper | Venue | Key Finding | Link |
|-------|-------|-------------|------|
| HyPoradise | NeurIPS 2023 | Generative > Ranking for ASR correction | https://arxiv.org/abs/2309.15701 |
| Whispering LLaMA | EMNLP 2023 | 37.66% WER improvement, cross-modal | https://arxiv.org/abs/2310.06434 |
| Task-Activating Prompting | Amazon 2023 | Better prompts for STT correction | assets.amazon.science |
| Thai Tone in ASR | ACL | Tone marks critical for Thai STT | Y14-1023 |
| CER for Multilingual | NAACL 2025 | CER ดีกว่า WER สำหรับ Thai | NAACL Findings 2025 |
| Typhoon2 | SCB10X 2024 | Thai-specialized LLM, 7B | https://huggingface.co/scb10x/typhoon2-qwen2.5-7b-instruct |
| OpenThaiGPT 1.5 | AIEAT 2024 | Thai-native 7B, 131K context | https://huggingface.co/openthaigpt/openthaigpt1.5-7b-instruct |

---

## 10. สรุปแนะนำสำหรับ MVP

### Stack ที่แนะนำ (เพิ่มจาก research เดิม)

```
เดิม:  Whisper STT → Text Inject
ใหม่: Whisper STT → LLM Correction (Qwen 3B / Ollama) → Text Inject
                          ↑
                   Context Manager
                   (rolling window + domain + user vocab)
```

### Priority การพัฒนา

**Phase 1 (MVP):**
- Whisper STT เท่านั้น (ไม่มี LLM)
- ทดสอบ pipeline พื้นฐาน

**Phase 2 (LLM Basic):**
- เพิ่ม Ollama integration (Qwen 3B)
- Basic filler word removal + punctuation
- Zero-shot correction

**Phase 3 (Context):**
- Rolling window context (3 ประโยค)
- Domain detection
- User vocabulary

**Phase 4 (Polish):**
- Few-shot examples ใน prompt
- Model selection UI
- Fast/Standard/Context mode toggle
- Auto-learn vocabulary
